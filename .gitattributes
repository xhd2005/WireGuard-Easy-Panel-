# 仓库统一以 LF 存储，并且强制 Windows 工作区也落 LF。
#
# 为什么必须显式钉住：本机 core.autocrlf=true，若不声明属性，任何一次
# git checkout / git stash / 在 Windows 上 clone 都会把 wg.sh 转成 CRLF，
# 脚本随即在 Linux 上报 "bad interpreter: /bin/bash^M" 而无法执行。
# 这个仓库正是以"解压一个目录"的形式分发的，所以该路径不是假设。
# 注意本文件只影响本仓库，不修改任何人的全局 git 配置。

*.sh          text eol=lf
*.conf        text eol=lf
*.go          text eol=lf
*.md          text eol=lf
*.yaml        text eol=lf
*.yml         text eol=lf
*.service     text eol=lf
.gitignore    text eol=lf

# golden 测试要求 fixture 字节完全一致（解析→写出→与输入比对）。
# -text 彻底禁用换行符转换，否则一次 smudge 就能让往返测试无故失败。
fixtures/**   -text

# 明确标记为二进制，避免被任何文本过滤误伤。
*.png         binary
*.zip         binary
